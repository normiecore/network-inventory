# network-inventory

MCE Network Inventory — a Rust daemon that ARP-sweeps the local network on
a schedule and serves a small web UI of discovered devices.

Development is happening on feature branches; see the open pull requests.
